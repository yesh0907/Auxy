// import { GET_USER } from '../actions';

const initialState = {
  value: '',
  evidence: []
};

export default function diagnosis(state=initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
